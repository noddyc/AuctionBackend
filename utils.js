let dayHour = 11; // utc
let dayMin = 45; // utc
let daySec = 40; // utc

// need to convert to utc
let nightHour = 21;
let nightMin = 17;
let nightSec = 40;

module.exports ={dayHour, dayMin, daySec, nightHour, nightMin, nightSec}
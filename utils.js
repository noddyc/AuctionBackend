let dayHour = 8; // should put in est time according to utc
let dayMin = 45; // utc
let daySec = 40; // utc

// need to convert to utc or not
let nightHour = 21;
let nightMin = 17;
let nightSec = 40;

module.exports ={dayHour, dayMin, daySec, nightHour, nightMin, nightSec}